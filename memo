custom npm 명령어 실행은 `npm run ~` 으로 실행하며, 내장 명령어 실행은 `npm ~` 으로 실행한다.


유의적 버전. version 

v16.12.0 일 경우,
메이저(major) 버전 : 16에 해당하는 부분. 기존 버전과 호환되지 않게 변경한 경우
마이너(minor) 버전 : 12에 해당하는 부분. 기존 버전과 호환되면서 기능이 추가된 경우
수(patch) 버전 : 0에 해당하는 부분. 기존 버전과 호환되면서 버그를 수정한 경우 

틸트 ~
마이너 버전이 명시되어 있으면 패치 버전을 변경
`~1.2.3` 표기는 `1.2.3`부터 `1.3.0`미만까지 포함
`~0` 표기는 `0.0.0`부터 `1.0.0` 미만까지 포함

캐럿 ^
정식버전에서 마이너와 패치 버전을 변경
`^1.2.3` 표기는 `1.2.3`부터 `2.0.0` 미만 까지를 포함
정식버전 미만인 0.x 버전은 패치만 갱신
`^0` 표기는 `0.0.0`부터 `0.1.0` 미만 까지를 포함

IIFE : 즉시 실행 함수
Global Scope 를 오염 시키지 않기 위해서 쓰는 함수
선언과 동시에 실행이 되며, 이름이 있다면 나중에 호출 할수도 있습니다.

모듈 시스템
exports, import 
몇몇 브라우저에서는 모듈 시스템을 지원하지 않고 있음. (ex. 인터넷 익스플로러)
그렇다면 모듈 시스템을 모든 브라우저에서 지원할 수는 없을까 ?

번들
의존성이 있는 js파일들을 모아주는 작업

install할 때 데브 관련 모듈 설치 시 devDependencies에 생김. 그렇지 않을 경우 dependencies에 생김
